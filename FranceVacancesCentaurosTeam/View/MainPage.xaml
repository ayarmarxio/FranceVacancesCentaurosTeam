<Page
    x:Class="FranceVacancesCentaurosTeam.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:FranceVacancesCentaurosTeam"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:data="using:FranceVacancesCentaurosTeam.Model"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="15*"/>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="15*"/>
        </Grid.ColumnDefinitions>

        <Grid.RowDefinitions>
            <RowDefinition Height="5*"/>
            <RowDefinition Height="30*"/>
            <RowDefinition Height="5*"/>
        </Grid.RowDefinitions>
        
        <!--<Grid.Background>
            <ImageBrush ImageSource="ms-appx:///Assets/FranceVacancesPortrait.jpg" Stretch="UniformToFill" />
        </Grid.Background>-->
   
        <!--<Image Source="ms-appx:///Assets/FRanceVacancesLogo1.png" Grid.Row="0" HorizontalAlignment="Center" Margin="50,0,50,0" Grid.Column="1" Stretch="UniformToFill"/>-->

        <GridView Grid.Column="1" Grid.Row="1" ItemsSource="{x:Bind Apartments}">
            <GridView.ItemTemplate>
                <DataTemplate x:DataType="data:Apartment">
                    <StackPanel>
                        <Image  Source="{x:Bind MainImage}" Height="300" Width="500"  />
                        <TextBlock FontSize="40" Text="{x:Bind Style}"/>
                        <TextBlock FontSize="20" Text="{x:Bind Price}"/>
                    </StackPanel>
                </DataTemplate>
            </GridView.ItemTemplate>
        </GridView>
        
        <!--<Button  Grid.Row="1" Width="300" VerticalAlignment="Top" HorizontalAlignment="Left" Height="70" Content="City" Click="Button_Click" Foreground="White" FontSize="40" />-->
        
        <!--<SplitView Name="MySplitView" DisplayMode="Overlay" IsPaneOpen="False" Grid.Row="1" >
            <SplitView.PaneBackground>
                <SolidColorBrush Color="Transparent"/>
            </SplitView.PaneBackground>
            <SplitView.Pane>
                <StackPanel Width="300" HorizontalAlignment="Left" Margin="0,75,0,0" >
                    <Button x:Name="Cannes" Content="Cannes" Foreground="White" FontSize="40"  FontFamily="Trebuchet MS" Height="70" Width="300"/>
                    <Button x:Name="Chamonix" Content="Chamonix" Foreground="White" FontSize="40" FontFamily="Trebuchet MS" Height="70" Width="300"/>
                    <Button x:Name="Lyon" Content="Lyon" Foreground="White" FontSize="40" FontFamily="Trebuchet MS" Height="70" Width="300"/>
                    <Button x:Name="Nice" Content="Nice" Foreground="White" FontSize="40" FontFamily="Trebuchet MS" Height="70" Width="300"/>
                </StackPanel>
            </SplitView.Pane>
        </SplitView>
        <Button  Grid.Row="1" Grid.Column="2" Width="300" VerticalAlignment="Top" HorizontalAlignment="Right" Height="70" Content="Apartment" Click="Button_Click_1" Foreground="White" FontSize="40"/>-->
        
        <!--<SplitView Name="MyApartmentSplitView" DisplayMode="Overlay" IsPaneOpen="False" Grid.Row="1" Grid.Column="3" HorizontalAlignment="Right">
            <SplitView.PaneBackground>
                <SolidColorBrush Color="Transparent"/>
            </SplitView.PaneBackground>
            <SplitView.Pane>
                <StackPanel Width="300" Margin="0,75,0,0" HorizontalAlignment="Right" >
                    <Button x:Name="Cottage" Content="Cottage" Foreground="White" FontSize="40"  FontFamily="Trebuchet MS" Height="70" Width="300"/>
                    <Button x:Name="Flat" Content="Flat" Foreground="White" FontSize="40" FontFamily="Trebuchet MS" Height="70" Width="300"/>
                    <Button x:Name="Villa" Content="Villa" Foreground="White" FontSize="40" FontFamily="Trebuchet MS" Height="70" Width="300"/>
                    <Button x:Name="Bungalow" Content="Bungalow" Foreground="White" FontSize="40" FontFamily="Trebuchet MS" Height="70" Width="300"/>
                </StackPanel>
            </SplitView.Pane>
        </SplitView>-->
      













        <!--<GridView Grid.Row="1" Grid.Column="1" ItemsSource="{x:Bind ApartmentList}">
            <GridView.ItemTemplate>
                <DataTemplate x:DataType="data:Apartment">
                    <StackPanel>
                        <Image Height="150" Source="{x:Bind Image}"/>
                        <TextBlock FontSize="40" Foreground="White" Text="{x:Bind Description}" />
                        <TextBlock FontSize="16" Foreground="White" Text="{x:Bind Price}"/>
                        <TextBlock FontSize="30" Foreground="White" Text="{x:Bind Style}"/>
                    </StackPanel>
                </DataTemplate>
            </GridView.ItemTemplate>
        </GridView>-->
    </Grid>
</Page>
